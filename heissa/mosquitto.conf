
# Place your local configuration in /etc/mosquitto/conf.d/
#
# A full description of the configuration file is at
# /usr/share/doc/mosquitto/examples/mosquitto.conf

#pid_file /run/mosquitto/mosquitto.pid
# Erlaubt Verbindungen auf allen Interfaces
#listener 1883 0.0.0.0
#listener 1883 127.0.0.1

## WireGuard für Remote-Zugriff
# 10.0.0.1 existiert nicht mehr (kein wg-IF) -> Bind schlug fehl
listener 1883 127.0.0.1
# Ab Mosquitto v2.0 muss man anonyme Verbindungen explizit erlauben, 
# wenn man kein Passwort nutzt
allow_anonymous true

persistence true
persistence_location /var/lib/mosquitto/

log_dest file /var/log/mosquitto/mosquitto.log

include_dir /etc/mosquitto/conf.d
